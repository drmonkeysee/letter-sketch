<svg xmlns="http://www.w3.org/2000/svg" stroke="black">
  <style>
    .sprite { display: none; }
    .sprite:target { display: inline; }
  </style>

  <g id="tool-point" class="sprite">
    <circle cx="22" cy="22" r="10" />
  </g>
  <!-- brush -->
  <!-- box brush -->
  <!-- fill -->
  <g id="tool-rect" class="sprite">
    <rect x="5" y="5" width="35" height="35" fill="none" />
  </g>
  <g id="tool-fill-rect" class="sprite">
    <rect x="5" y="5" width="35" height="35" fill="black" />
  </g>
  <!-- box rect -->
  <!-- ellipse -->
  <!-- fill ellipse -->
  <!-- line -->
  <!-- text -->
  <!-- swap -->
  <!-- eyedrop -->
</svg>
